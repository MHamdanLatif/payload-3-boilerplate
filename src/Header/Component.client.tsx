// Deprecated: header logic now lives in ./Component.tsx as a single client component.
// Kept as a thin re-export so any straggler imports continue to resolve.
export { Header as HeaderClient } from './Component'
